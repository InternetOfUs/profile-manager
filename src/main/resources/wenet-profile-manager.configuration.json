{
	"version": {
		"api": "${api-version}",
		"vendor": "UDT-IA, IIIA-CSIC",
		"software": "${project.version} (${maven.build.timestamp})"
	},
	"api": {
		"host": "localhost",
		"port": 8080
	},
	"persistence": {
		"database": "profile_manager",
		"host": "localhost",
		"port": 5432,
		"user": "profile_manager",
		"password": "password",
		"flyway": {
			"baselineOnMigrate": true,
			"baselineDescription": "WeNet profile manager",
			"encoding": "UTF-8"
		}
	}
}